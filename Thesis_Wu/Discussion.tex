\todo[inline, color=green!40]{探討每個experimental result小節的原因}

\section{Contributions of each part in neural decoder}
\begin{multicols}{2}
\todo[inline, color=green!40]{探討neural decoder的每個元件的貢獻}
\end{multicols}

\subsection{Effect of layer normalization}
\begin{multicols}{2}
\todo[inline, color=green!40]{LN 對於訓練模型收斂速度的提升}
The effect of layer normalization in the position, velocity and acceleration decoders is showed in Figure~\ref{fig:loss_vs_epoch}.
These diagrams help us analysis the model based on the how many epoch numbers are required for the testing loss to converge.
In the position and velocity decoders, it seems that implementing layer normalization does not contribute to a better convergence significantly since two classes of curve overlap.
However, for acceleration decoders, the loss dropped drastically with the aid of layer normalization compared to original GRU.
\end{multicols}

\begin{figure}[H]
    \begin{center}
        \begin{subfigure}{0.48\textwidth}
            \includegraphics[width=1\textwidth]{loss_to_epoch_position.png}
            \caption{Position}
        \end{subfigure}
        \begin{subfigure}{0.48\textwidth}
            \includegraphics[width=1\textwidth]{loss_to_epoch_velocity.png}
            \caption{Velocity}
        \end{subfigure}
        \begin{subfigure}{0.48\textwidth}
            \includegraphics[width=1\textwidth]{loss_to_epoch_acceleration.png}
            \caption{Acceleration}
        \end{subfigure}
        \captionsetup{width=0.5\textwidth}
        \caption{Epoch vs Loss Graphs from 3 different kinematic variable decoders}
        \label{fig:loss_vs_epoch}
    \end{center}
\end{figure}

\begin{multicols}{2}
The reason for ordinary GRU failed to decode the acceleration is mainly caused by the wide range of this kinematic variable.
When it comes to decoding fingertip position, the labels are distributed mainly between $100\sim-50$.
But for velotiy the distribution is between $200\sim-200$ and acceleration $2000\sim-2000$, whereas most values are approximately zero.
It is unlikely for a deep learing network to adapt this drastic range of distribution.
But with the help of layer normalization by optimizing the distribution of inner GRU dynamics,
the epoches for training loss needed converge drops, making the training process efficient.
Decoding accuracy also improved with a considerable degree.
As we showed in the Table~\ref{tab:ablation_study} from Section~\ref{sec:ablation_study}, 
the weighted average $R^2$ across all 47 sessions in x-axis position and both axes of acceleration rised.


\end{multicols}

\subsection{Effect of temporal self-attention}
\begin{multicols}{2}
\todo[inline, color=green!40]{ 比較有/無 attention 的預測軌跡 }
\end{multicols}


\section{The reason why our neural decoder surpasses others}
\begin{multicols}{2}
\todo[inline]{探討neural decoder比其他方法好的原因}
Our attention mechanism modified the method of combining the attention map and the GRU output feature map together from \cite{lin2017structured}, 
using the scaling technique similar to \cite{hu2018squeeze}.
On the other hand we preserve the benefit of different attention aspects, similar to the \textit{Multi-head attention} ideal in \cite{vaswani2017attention}, 
annotating the same sequence with multiple attention vectors.


\end{multicols}


\section{The insight our attention module provide}
\begin{multicols}{2}
\todo[inline, color=green!40]{探討attention找到了甚麼重點?}
Our temporal attention mechanism can find the different pattern in different kinematic variables.
Especially it is the point-to-point movement we are trying to decode, there exists no regular pattern in the trajectories.
With the help of attention mechanism, the model can somehow 
\end{multicols}


\section{Connection between neural network and actual brain function}
\begin{multicols}{2}
\todo[inline, color=green!40]{探討網路內部的運作方式跟大腦的關連}
The attention mechanism acts as a visualization tool for depicting how the model sees.
Just like human brain, first we have to browse trough the whole sequence of the data, 
then by our own experiacne to 

\todo[inline]{\cite{glaser2019roles}}

\end{multicols}


\section{Reason behind failure cases}
\begin{multicols}{2}
\todo[inline, color=green!40]{探討失敗例子的原因}
\begin{itemize}
    \item \textbf{Wrong weights for the two loss functions}\\
    we have two types of loss functions, MSE and the penalization term.
    MSE represents the error between the predicted kinematic variables and its observed value, 
    The penalization term 
    \item \textbf{Summing the feature map from different attention vectors together}\\
    When we tried to cut the number of parameters in the fully connection layers,

\end{itemize}
\end{multicols}

